{
  "$schema": "https://turbo.build/schema.json",
  "globalEnv": ["APP_NAME", "PORT"],
  "tasks": {
    "dev": { "persistent": true, "cache": false },
    "build": { "outputs": ["dist/**"] },
    "lint": { "dependsOn": ["^lint"] },
    "clean": { "cache": false }
  }
}
