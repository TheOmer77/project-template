{
  "name": "backend",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon",
    "build": "pnpm --silent run lint && etsc --config=etsc.config.mjs",
    "lint": "tsc && eslint --max-warnings=0 src"
  },
  "dependencies": {
    "@hono/node-server": "^1.9.1",
    "@theomer77/some-package": "^1.0.0",
    "hono": "^4.2.1"
  }
}
