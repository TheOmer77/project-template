{
  "name": "backend",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "nodemon",
    "build": "npm run lint -s && etsc --config=etsc.config.mjs",
    "lint": "tsc && eslint --max-warnings=0 src"
  },
  "dependencies": {
    "@theomer77/some-package": "^1.0.0",
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
