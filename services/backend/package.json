{
  "name": "@repo/backend",
  "version": "0.0.0",
  "main": "src/index.ts",
  "scripts": {
    "start": "node dist/index.js",
    "dev": "tsx watch --inspect=0.0.0.0 src/index.ts",
    "build": "pnpm --silent run lint && tsx build.ts",
    "lint": "tsc && eslint --max-warnings=0 src",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@hono/node-server": "^1.13.0",
    "@repo/shared": "workspace:*",
    "hono": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.5.5",
    "esbuild": "^0.23.1",
    "eslint": "^8.57.1",
    "tsup": "^8.3.0",
    "tsx": "^4.19.1"
  }
}
